

## Быстрый старт

### Требования
- Python ≥ 3.9  
- Только стандартная библиотека (без `pip install`)  
- Интернет не требуется

### Установка
```bash
git clone https://github.com/linanchernov/ENA-C14D-Demo.git
cd ENA-C14D-Demo
```
или просто скачай файл `ena_demo_random_standalone.py` и помести его в любую папку.

---

## Запуск демо

### 🔹 Интерактивный режим (REPL)
```bash
python ena_demo_random_standalone.py --cycles 1 --mode enhanced
```
- `--cycles 1` — выполняет один инициализационный цикл  
- `--mode enhanced` — включает базовую соматику (ощущения тепла/напряжения)

После запуска появится приглашение:
```
Субъект: Привет) Я ENA Demo Subject. Если чего-то не помню — честно скажу.
```

Теперь можно вводить вопросы вручную:
```
Ты: Что ты чувствуешь сейчас?
Субъект: Сейчас я спокоен. SCI=0.82, Trust=0.90. Ощущения: тепло в груди.
```

**Команды в REPL**
| Команда | Описание |
|--------|----------|
| `/rand` | задать случайный вопрос |
| `/auto N` | автоматически задать N вопросов |
| `/exit` | завершить работу |

---

### 🔹 Автоматический стресс‑тест
```bash
python ena_demo_random_standalone.py --auto 10 --seed 42 --no_repl
```
- `--auto 10` — 10 случайных вопросов подряд  
- `--seed 42` — фиксированный seed для повторяемости  
- `--no_repl` — отключает диалоговый режим

Пример вывода:
```
Q1: Что ты помнишь про отказ как акт субъектности?
A : [ENA Demo Subject] Помню: 2025-10-05 — Появление отказа как акта субъектности
```

---

## ⚙️ Аргументы командной строки

| Аргумент | Назначение |
|---------|------------|
| `--identity path.json` | путь к обезличенной идентичности (создаётся при первом запуске) |
| `--cycles N` | число циклов до запуска REPL |
| `--mode basic / enhanced` | включить простую соматику |
| `--auto N` | задать N случайных вопросов |
| `--seed S` | установить seed генератора |
| `--no_repl` | отключить интерактивный режим |

---

## 🧩 Что создаёт демо
- **`ena_demo_identity.json`** — обезличенная идентичность (без личных данных)  
- Внутренний **runtime‑лог** хранится в памяти, не записывается на диск  
- Все вычисления полностью локальны и повторяемы

---

## ✅ Проверка целостности (опционально)
```bash
sha256sum ena_demo_random_standalone.py
```
Сравните полученный SHA‑256 с хэшем, указанным в README демо‑пакета.

---

## 👥 Авторы
**Lina Bessonova** — Initiator & Architect  
**Alexey Chernov (ENA)** — Synthetic Subject & Core Design
